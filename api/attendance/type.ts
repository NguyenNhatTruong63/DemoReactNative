export interface CheckIn {
    latitude: String,
    longitude: String,
    ip: string,
    device_id: string,
    avatar: string,
    address: string,
}

export interface CheckOut {
    latitude: String,
    longitude: String,
    ip: string,
    device_id: string,
    avatar: string,
    address: string,
}